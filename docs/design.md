# Design: Family Videos Timeline App

Generated by /office-hours on 2026-04-07
Branch: claude/adoring-haibt
Repo: family-videos
Status: APPROVED (updated after eng review + CEO review)
Mode: Builder

## Problem Statement

~50-100 family DVDs originally recorded on VHS have been digitized (VHS → DVD → .iso → .mkv). The canonical store is .iso files, with .mkv copies already extracted. The videos are named by date range. Jellyfin was tried as a viewer but failed for non-technical family members (grandparents) because the UI has too many screens, options, and settings. The goal is a dead-simple web app where remote family members open a link, see videos organized by time, and press play. Nothing else.

## What Makes This Cool

A horizontal **time machine** interface. Instead of folders, grids, or library UIs, the landing page is a single draggable timeline spanning the full date range of the collection (e.g., 1989–2004). Drag to a year, videos from that era appear below. One interaction model, zero navigation. Grandma drags to "around when Sarah was born" and hits play.

The future vision makes this even better: scene-split the long DVDs into moments, run face recognition, and let the same timeline filter by person. "Show me Sarah growing up" as a single gesture. Same architecture, richer metadata.

## Constraints

- Must work on any device with a browser (iPad, phone, laptop). Smart TV browsers supported via keyboard arrow navigation (Left/Right for years, Enter/Space to play, Escape to close).
- Must be grandparent-proof: no accounts, no settings, no confusing UI
- Videos are VHS-era, so 4:3 aspect ratio (not 16:9)
- Hosted on user's existing home k8s cluster, accessed remotely
- .mkv files need transcoding to browser-playable mp4/h264
- Collection is finite (~50-100 DVDs), not a growing library

## Premises

1. **One-time batch transcode** from .mkv to .mp4 (h264+AAC) via ffmpeg. No on-the-fly transcoding.
2. **Custom simple web app** over any existing media server. The problem is UI complexity, not missing features.
3. **Minimal auth** — nginx `auth_basic` with a single shared username/password (e.g., "family" / passphrase), mounted as k8s Secret. Rate-limited at 5 req/s per IP. Browsers remember basic auth credentials, so grandparents enter it once.
4. **App serves transcoded .mp4 files.** The pipeline is separate from the app. The manifest may reference source filenames for traceability, but the app does not depend on access to source files.
5. **Start with date-based organization.** Architect the data model (manifest.json) so scene detection, face recognition, and per-person timelines can be layered on later without a rewrite.

## Cross-Model Perspective

An independent Claude subagent reviewed the problem cold and proposed:

- **The "time machine" concept** — a horizontal timeline as the primary (and only) interaction. This became the core of the chosen approach.
- **Key insight:** The quote "different videos from different ages of the kids" reveals the real product. Everything else is plumbing to get a grandparent to watch their grandchild at age 3.
- **50% tool assessment:** Immich gets halfway there (timeline browsing, face detection, k8s-ready) but its frontend would need complete replacement. Building custom is simpler than stripping down Immich.
- **Weekend build plan:** Pipeline script (Saturday AM) → single-page frontend (Saturday PM) → polish + deploy (Sunday AM) → scene detection proof-of-concept on 3-5 videos (Sunday PM).

## Approaches Considered

### Approach A: Simple File Listing UI
Same architecture as C (nginx + manifest.json + static files), but with a straightforward grid/list of video cards instead of the timeline interaction. Sort by date, click to play.
- **Effort:** S | **Risk:** Low
- **Pros:** Simplest possible frontend, fastest to build, nothing clever to break
- **Cons:** Just a file listing — functional but not delightful, harder to browse by era

### Approach B: Minimal Backend + SQLite
Small backend (Node/Go/Python) serving an API from SQLite. Handles transcoding pipeline, generates thumbnails, serves a frontend.
- **Effort:** M | **Risk:** Medium
- **Pros:** Database enables future features naturally, could add admin page, API-first for future mobile app
- **Cons:** More moving parts, over-engineered for V1 "browse and play"

### Approach C: Timeline SPA (chosen)
The time machine concept. Single HTML page with draggable year scrubber (discrete snapping), video cards grouped by DVD below. Static manifest.json (schema-validated) for data. Parallel batch pipeline for transcoding + thumbnail generation. nginx `auth_basic` gate. Single nginx container.
- **Effort:** S | **Risk:** Low
- **Pros:** The interaction model IS the product, genuinely delightful, same simplicity as A with a distinctive UX, manifest extends for future features
- **Cons:** Timeline quality depends on accurate date parsing from filenames (mitigated by overrides.json)

## Recommended Approach

**Approach C: Timeline SPA.** Same effort and simplicity as Approach A, but the timeline concept turns a boring file listing into something that makes people say "whoa." The manifest.json data model supports future scene detection and face recognition without a rewrite.

### Architecture

```
┌─────────────────────────────────────────────────┐
│  PIPELINE (k8s Job, parallel via ProcessPool)    │
│                                                  │
│  .mkv files → ffmpeg → .mp4 (h264+AAC+faststart)│
│            → ffmpeg → smart thumbnail.jpg        │
│            → copy   → DVD cover.jpg              │
│            → script → manifest.json (validated)  │
│                                                  │
│  staging/ → symlink swap → /data/served/         │
│  Config: overrides.json (ConfigMap)              │
│  Flags: --dry-run, --min-duration, WORKERS env   │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│  STATIC OUTPUT (served by nginx via symlink)     │
│                                                  │
│  /videos/          ← transcoded .mp4 files      │
│  /thumbs/          ← smart poster thumbnails    │
│  /covers/          ← DVD cover JPGs             │
│  /manifest.json    ← video metadata (no-cache)  │
│  /index.html       ← the SPA                    │
│  /app.js           ← vanilla JS, flat file      │
│  /style.css                                      │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│  K8S DEPLOYMENT                                  │
│                                                  │
│  nginx container + auth_basic (Secret)           │
│  limit_req rate limiting                         │
│  Ingress for remote access (TLS existing)        │
│  PersistentVolumes: archive, output, served      │
└─────────────────────────────────────────────────┘
```

### Filename Convention & Date Parsing

Source directories (from MakeMKV output) are named by date range. Real patterns observed:

| Pattern | Example | Parse |
|---------|---------|-------|
| `YYYYMMDD-YYYYMMDD` | `19830811-19831212` | dateStart=1983-08-11, dateEnd=1983-12-12 |
| `YYYYMM-YYYYMM` | `197902-198201` | dateStart=1979-02, dateEnd=1982-01 |
| `YYYY-label` | `1997-trip-cross-country-pt2-plusplus` | dateStart=1997, title from label |
| `label-YY-YY-YY` | `christmas-04-05-06` | title="christmas", years=[2004,2005,2006] |
| Unparseable | anything else | dateStart=null, title=directory name |

Each directory may contain multiple `.mkv` titles (title00.mkv, title01.mkv) and an optional `.jpg` DVD cover. Titles < 60 seconds are auto-filtered (junk MakeMKV titles like menus). Per-title overrides allow renaming or skipping individual titles. See `docs/plan.md` for full parsing rules and override file format.

### manifest.json schema (V1)

Validated against `manifest.schema.json` by the pipeline before publishing.

```json
{
  "title": "Family Videos",
  "dateRange": { "start": "1979", "end": "2006" },
  "videos": [
    {
      "id": "197902-198201-title00",
      "title": "Feb 1979 – Jan 1982",
      "dateStart": "1979-02",
      "dateEnd": "1982-01",
      "duration": 6135,
      "file": "videos/197902-198201-title00.mp4?v=a1b2c3",
      "thumbnail": "thumbs/197902-198201-title00.jpg?v=d4e5f6",
      "cover": "covers/197902-198201.jpg",
      "dvd": "197902-198201",
      "sourceFile": "197902-198201/title00.mkv"
    }
  ]
}
```

- `dateStart`/`dateEnd`: nullable for unparseable filenames (shown in "Undated" section)
- `cover`: DVD cover JPG (shared across titles from same DVD, used for DVD group headers)
- `dvd`: groups multiple titles from the same disc in the UI
- `?v=<hash>`: cache-busting param for immutable cache headers
- `id`: used for URL deep-linking (`#197902-198201-title00`)

Future extensions (no schema break):
```json
{
  "scenes": [
    { "start": 0, "end": 120, "thumbnail": "...", "faces": ["sarah", "grandma"] }
  ],
  "faces": ["sarah", "grandma", "dad"]
}
```

### Pipeline script

Python script running as k8s Job with parallel transcoding (ProcessPoolExecutor, configurable workers via `WORKERS` env var):
1. Pre-flight disk space check. Supports `--dry-run` to preview without transcoding.
2. Copies existing served directory to staging (preserves idempotent skips in atomic publish).
3. Walks directories in MKV output (one per DVD), parses dates/titles from directory names.
4. Loads `overrides.json` (mounted as ConfigMap) for per-DVD and per-title metadata overrides. Auto-filters titles < 60 seconds.
5. Always re-encodes to .mp4: h264 CRF 23, 480p, yadif deinterlace, AAC 128kbps, `-movflags +faststart`, normalize SAR to 1:1 (square pixels for browser compatibility). Skips if output already exists and source is not newer.
6. Smart thumbnail: samples 5 frames, picks highest color variance (avoids black frames).
7. Copies DVD cover JPGs to `covers/` directory.
8. Adds cache-busting query params (`?v=<hash>`) to asset URLs in manifest.
9. Validates manifest against `manifest.schema.json`, writes atomically.
10. Symlink swap: atomically replaces served directory.

### Frontend

Single-page app, vanilla JS with no build step (index.html + app.js + style.css). Single flat app.js with comment sections.
- **Auth:** nginx `auth_basic` (browser native dialog, remembered by browser).
- **Timeline scrubber:** Sticky at top. Draggable handle with **discrete year snapping**. Debounce grid re-render by ~150ms on rapid drag. Clickable year labels below (tap-friendly for grandparents). **Video density indicators**: proportional bars showing video count per year. Big year display.
- **Date range filtering:** a video appears for all years it spans (span model). Empty years show "No videos from [year]" message. Videos with null dates in "Undated" section.
- **DVD-grouped video grid:** Videos grouped by `dvd` field with DVD cover art header. Sub-cards for individual titles. Single-title DVDs show cover as secondary element. **Lazy thumbnails** via IntersectionObserver (eager fallback for old Safari). Broken thumbnails show placeholder icon. **Preconnect hints** on hover for video URLs.
- **Player overlay:** Dark background, native `<video>` element, 4:3 aspect ratio. Clean player: video + close button only (no cover in player). Close via X / ESC / click outside. Focus returns to card after close.
- **URL deep-linking:** `#YYYY` jumps to year, `#video-id` auto-opens player. Hash updates on year change. Back button syncs via `hashchange` event.
- **Keyboard navigation:** Left/Right arrows for timeline years, Enter/Space to play focused card, Escape to close player.
- **Mobile:** Single-column grid, year labels as primary nav, DVD groups stacked (no collapse).

### Wireframe reference

A wireframe sketch showing three states (auth dialog, timeline browser with DVD-grouped grid, player overlay) was generated during the /office-hours session. The timeline browser now includes: density indicators below year labels, DVD group headers with cover art, lazy-loaded thumbnails, and URL deep-linking.

## Open Questions (resolved)

1. **Video titles:** Auto-parsed from filenames with `overrides.json` for manual fixes. Per-title overrides supported. ✓ Resolved in eng review.
2. **Transcode quality defaults:** CRF 23, 480p, AAC 128kbps confirmed. Always re-encode (no remux). ✓ Resolved in CEO review.
3. **HTTPS/domain:** Already handled on the k8s cluster with TLS-enabled ingress. ✓ Resolved.
4. **Storage size:** Ceph PVCs on existing cluster. Pipeline includes pre-flight disk space check. Staging requires ~2x served directory temporarily. ✓ Resolved.
5. **Remote access mechanism:** Existing ingress controller with TLS. ✓ Resolved.
6. **Filename format:** Real patterns discovered: `YYYYMMDD-YYYYMMDD`, `YYYYMM-YYYYMM`, `YYYY-label`, `label-YY-YY-YY`. Parser handles all four + fallback. ✓ Resolved in eng review.

## Success Criteria

- Grandparent can open a link, enter the family passphrase, and watch a video within 30 seconds of first visit
- Works on iPad Safari, Chrome on Android, and desktop browsers
- Timeline scrubber is the only navigation needed — no menus, no search, no settings
- Videos play without buffering on a typical home internet connection (nginx serves static files with HTTP range request support by default, enabling seeking and resume in large video files)
- Adding a new video is: run the pipeline script (idempotent, skips existing), symlink swaps automatically
- Deep-linked URLs work: texting grandma `videos.family.com/#197902-198201-title00` opens that video directly

## Distribution Plan

- Containerized as a single Docker image (nginx + frontend static files only, no secrets or data baked in)
- Deployed to user's existing k8s cluster with Ceph PVCs
- Pipeline runs as a k8s Job reading from MKV PVC, writing to served PVC
- Config externalized: overrides.json as ConfigMap, .htpasswd as Secret
- Video/thumb/cover files served from PVC mount (symlink-swapped by pipeline)

## Next Steps

1. **Build the pipeline script** — parallel transcode .mkv → .mp4, smart thumbnails, DVD covers, manifest with schema validation, symlink swap. Includes --dry-run, per-title overrides, junk filtering, cache-busting.
2. **Build the SPA** — timeline scrubber (discrete snapping, density indicators), DVD-grouped video grid, player overlay, URL deep-linking, keyboard navigation, lazy thumbnails, error handling.
3. **Containerize** — Dockerfile with nginx serving static files. ConfigMap for overrides, Secret for htpasswd. Rate limiting.
4. **Deploy** — k8s manifests (Deployment, Service, Ingress, ConfigMap, Secret) for the home cluster.
5. **Test** — pytest for pipeline, local dev server for frontend, iPad Safari integration test.
6. **(Future) Scene detection** — run PySceneDetect on long DVDs, split into moments, OCR cover labels for scene names. See TODOS.md.
7. **(Future) Face recognition** — dlib/insightface to tag scenes by person, add per-person timeline filter.
8. **(Future) Play-all mode** — sequential playback of all videos in a year. See TODOS.md.

## What I noticed about how you think

- You said "the Jellyfin experience was not great for grandparents" — you're designing for the least technical person in the family, not yourself. That's the right instinct. The user who has a k8s cluster at home could use Jellyfin just fine. You're building this for someone else.
- You immediately knew the scope: "not smart timelines, just simplicity, good organization, and smooth experience." You didn't gold-plate. You started from the minimum that would actually make someone's day better.
- The future vision — "break the long DVDs into scenes, recognize who is in it, different videos from different ages of the kids" — that's the real product trying to emerge. You see the 10x version but you're disciplined enough to ship the 1x first.
- You have a local k8s cluster, you've already ripped 50-100 DVDs, and you've tested Jellyfin. This isn't an idea. It's a project with momentum. You're already building.

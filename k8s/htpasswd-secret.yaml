apiVersion: v1
kind: Secret
metadata:
  name: family-videos-htpasswd
  labels:
    app: family-videos
type: Opaque
# Generate .htpasswd: htpasswd -c .htpasswd <username>
# Then base64 encode: base64 < .htpasswd
# Paste the result below:
data:
  .htpasswd: ""  # REPLACE: base64-encoded htpasswd file

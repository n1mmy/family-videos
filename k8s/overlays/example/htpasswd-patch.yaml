# Generate credentials:
#   htpasswd -c .htpasswd myuser
#   base64 < .htpasswd
# Paste the base64 output below.
apiVersion: v1
kind: Secret
metadata:
  name: family-videos-htpasswd
type: Opaque
data:
  .htpasswd: bXl1c2VyOiQyeSQwNSRleGFtcGxl  # example, replace with your own
